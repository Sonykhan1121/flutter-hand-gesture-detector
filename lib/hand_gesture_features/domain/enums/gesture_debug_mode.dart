/// The one gesture-family diagnostic painter allowed on the live preview.
enum GestureDebugMode {
  off,
  direction,
  punch,
  zoomIn,
  zoomOut,
  returnMain,
  recording,
  callMe,
  followObject,
}
